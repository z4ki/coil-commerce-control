{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "sql",
  "description": "SQL database access capability",
  "windows": ["main"],
  "permissions": ["sql:default"],
  "platforms": ["windows", "macOS", "linux"]
}
